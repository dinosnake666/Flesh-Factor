#include <stdio.h>
#include <ctype.h>


/*void cleaner(void)
{
    int c;
    while ((c = getchar()) != '\n' && c != EOF);
}*/

int main (void)
{
    ft_strlen_check();

    printf("%s", ft_strlen_check());
    return(0);
}