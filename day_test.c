#include <stdio.h>

int main()
{
    int day;
    int bread;
    //unsigned int hp;

    day = 7;
    bread = 10;
    while (day > 0)
    {
        printf("Day : %d\n", day);
        day--;
        printf("Do you want to eat bread ? : ");
        scanf()
    }
    if (day == 0)
    {
        printf("Day : %d        This week has ended.", day);
    }
}